(() => {
    // Ejemplo
    // Archivos a evaluar - files to evaluate
    const filesToEvaluate = [
        { id: 1, flaggedToDelete: false },
        { id: 2, flaggedToDelete: false },
        { id: 3, flaggedToDelete: true },
        { id: 4, flaggedToDelete: false },
        { id: 5, flaggedToDelete: false },
        { id: 7, flaggedToDelete: true },
    ];

    // Archivos marcados para borrar - files to delete
    const files = filesToEvaluate.map((file) => file.flaggedToDelete);

    class AbstractUser {}
    class UserMixin {}
    class UserImplementation {}
    interface IUser {}

    // Mejor
    class User {}
    interface User {}

    // Todo: Tarea

    const TodaysDate = new Date();

    const elapsedDays: number = 23;

    const filesOnDirectory = 33;

    const name = 'Fernando';

    const lastName = 'Herrera';

    const daysSinceLastModified = 12;

    const StudentClassesMax = 6;
})();
